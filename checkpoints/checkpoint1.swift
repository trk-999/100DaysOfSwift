/*
 First checkpoint in learning swift. An
 unfortunate hiatus occurred as I was
 learning that had a 3 DAY delay in the
 100 day learning course. This is the
 first attempt after not touch swift code
 for a bit.
 */

import Cocoa

let temperature = 32.5
var conversion = temperature * 9 / 5 + 32

print("The current celsius temperature is \(temperature)°C while in Farenheit it is \(conversion)°F")
